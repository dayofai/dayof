# Ticket Panel – Future Notes

## Preference Service

Some UI/style/config preferences may come from a backend Preference Service instead of the DB. Treat as a separate server-owned source and merge with event defaults.
