{
  "routes": [
    { "handle": "filesystem" },
    { "src": "/_serverFn/(.*)", "dest": "/_serverFn/$1" },
    { "src": "/api/(.*)", "dest": "/api/$1" },
    { "src": "/.*", "dest": "/_shell.html" }
  ]
}
