{
  "name": "auth",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "turbo build --filter=database --filter=inngest && mkdir -p .output && touch .output/.keep",
    "dev": "bun src/dev.ts"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "better-auth": "^1.3.18",
    "@better-auth/expo": "^1.3.18",
    "hono": "^4.9.9",
    "database": "workspace:*",
    "inngest-kit": "workspace:*"
  },
  "devDependencies": {
    "@hono/node-server": "^1.19.4",
    "typescript": "^5.9.2"
  },
  "turbo": {
    "tasks": {
      "build": {
        "outputs": []
      }
    }
  }
}
