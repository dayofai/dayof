{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "regions": ["iad1"],
  "functions": {
    "api/index.ts": {
      "maxDuration": 300
    }
  },
  "ignoreCommand": "if [ -z \"$VERCEL-GIT-PREVIOUS-SHA\" ]; then exit 1; fi; git diff --quiet \"$VERCEL_GIT_PREVIOUS_SHA\" \"$VERCEL_GIT_COMMIT_SHA\" -- apps/auth || exit 1"
}
